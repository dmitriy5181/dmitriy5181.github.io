kind: pipeline
name: default

steps:
  - name: build
    image: dmitriy5181/hugo
    volumes:
      - name: www
        path: /var/www/blog
    commands:
      - find /var/www/blog -type f -delete
      - hugo --destination /var/www/blog

volumes:
- name: www
  host:
    path: /var/www/blog